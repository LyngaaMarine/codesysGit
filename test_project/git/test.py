from __future__ import print_function

import itertools
import string

project = projects.primary
finds = project.find("Library Manager", True)[0]

print(librarymanager.repositories[0])

# lib = librarymanager.find_library("SysFile, 3.5.17.0 (System)")
# print(lib[0])

# libs = librarymanager.get_all_libraries(True)
# for lib in libs:
#     print(lib)
